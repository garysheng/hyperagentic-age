# the-makers — generation prompts

Register anchor (`reference/style/warm-editorial-neutral/refs/warm-editorial-swatch.png`) is passed FIRST as the style anchor on every shot. warm editorial ink-and-wash; never neon, 3D/CGI/Pixar, glossy plastic, HUD/arc-reactor comic vocabulary, neo-comic.

TODO(author): replace each body below. A prompt must (a) lead with the register anchor, (b) bake the rejected poles as negatives, (c) state the invariants that must not drift, and (d) contain no legible text unless the design calls for it.

REQUIRED before any render: `forward-fullbody`, `face-neutral`. Shoot those first, then chain the rest off them so identity holds.

## face-neutral  -> reference/the-makers/face-neutral.png
TODO(author): the prompt for this shot.

## face-3q  -> reference/the-makers/face-3q.png
TODO(author): the prompt for this shot.

## expressions  -> reference/the-makers/expressions.png
TODO(author): the prompt for this shot.

## forward-fullbody  -> reference/the-makers/forward-fullbody.png
TODO(author): the prompt for this shot.

## profile-left  -> reference/the-makers/profile-left.png
TODO(author): the prompt for this shot.

## profile-right  -> reference/the-makers/profile-right.png
TODO(author): the prompt for this shot.

## back  -> reference/the-makers/back.png
TODO(author): the prompt for this shot.

## signature-pose  -> reference/the-makers/signature-pose.png
TODO(author): the prompt for this shot.
