# witney — generation prompts

BLOCKED ON A LOOK DECISION (2026-09-24). Witney's face, hair, age range and wardrobe are undecided. The comparison set is at `candidates/witney/looks-2026-09-24/`. Do not write or shoot these bodies until Gary picks a look; then seed the matrix from the CHOSEN roll (copied as-is, never regenerated), and state that look as invariants in every body below.

Register anchor (`reference/style/warm-editorial-neutral/refs/warm-editorial-swatch.png`) is passed FIRST as the style anchor on every shot. warm editorial ink-and-wash; never neon, 3D/CGI/Pixar, glossy plastic, HUD/arc-reactor comic vocabulary, neo-comic.

TODO(author): replace each body below. A prompt must (a) lead with the register anchor, (b) bake the rejected poles as negatives, (c) state the invariants that must not drift, and (d) contain no legible text unless the design calls for it.

REQUIRED before any render: `forward-fullbody`, `face-neutral`. Shoot those first, then chain the rest off them so identity holds.

## face-neutral  -> reference/witney/face-neutral.png
TODO(author): the prompt for this shot.

## face-3q  -> reference/witney/face-3q.png
TODO(author): the prompt for this shot.

## expressions  -> reference/witney/expressions.png
TODO(author): the prompt for this shot.

## forward-fullbody  -> reference/witney/forward-fullbody.png
TODO(author): the prompt for this shot.

## profile-left  -> reference/witney/profile-left.png
TODO(author): the prompt for this shot.

## profile-right  -> reference/witney/profile-right.png
TODO(author): the prompt for this shot.

## back  -> reference/witney/back.png
TODO(author): the prompt for this shot.

## signature-pose  -> reference/witney/signature-pose.png
TODO(author): the prompt for this shot.
